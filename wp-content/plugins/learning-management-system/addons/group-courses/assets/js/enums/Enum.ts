export enum GroupStatus {
	Any = 'any',
	Publish = 'publish',
	Draft = 'draft',
	Trash = 'trash',
}

export enum GroupDisplayStatus {
	Active = 'active',
	Pending = 'pending',
	Inactive = 'inactive',
}
